import React from 'react';
import Navigator from './routes/drawer';

/*
 *) Modals:
    -> In here we will try to create some kind of form to add data into it and create a new Review data
    -> and we will create Modal component inside 'Home' Screen
 */

const App = () => {
  return <Navigator />;
};

export default App;
