import React from 'react';
import Navigator from './routes/drawer';

/*
  *) Image in React-Native:
      -> place all the images inside the './assets' folder in root directory
      -> and use it using 'Image' component
      -> and we also had use it inside 'ReviewDetail.js' Component
*/

const App = () => {
  return <Navigator />;
};

export default App;
